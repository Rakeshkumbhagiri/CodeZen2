const TopicCards = ({ topics, onComplete }) => {
  return (
    // <div className="grid grid-cols-1 gap-6 mt-6 sm:grid-cols-2 md:grid-cols-4">
    //   {topics.map((topic) => (
    //     <div
    //       key={topic}
    //       className="p-5 text-center bg-white rounded shadow"
    //     >
    //       <h3 className="mb-3 text-lg font-semibold">{topic}</h3>
    //       <button
    //         onClick={() => onComplete(topic)}
    //         className="px-4 py-2 text-white rounded bg-slate-900 hover:bg-slate-700"
    //       >
    //         Complete Coding
    //       </button>
    //     </div>
    //   ))}
    // </div>
    <h1>Topic Card is  now Working </h1>
  );
};

export default TopicCards;
